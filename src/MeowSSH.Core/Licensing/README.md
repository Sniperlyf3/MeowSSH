# Licensing trust boundary

Paid access is granted only from a server-verified, cryptographically signed entitlement. Google Play purchase data collected on-device is an input to verification, not an entitlement by itself.

The app may cache a previously verified grant for offline use until its signed expiry time. Missing configuration, missing purchases, expired grants, invalid signatures, invalid package names, and invalid tiers all fail closed.
