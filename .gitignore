# Build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user
*.suo

# Test + coverage
[Tt]est[Rr]esult*/
coverage*.xml
coverage*.json
*.trx

# Tooling
.vs/
.vscode/
.idea/
node_modules/
.playwright/

# Local secrets and vaults -- never commit either
*.meowvault
appsettings.Local.json
local.settings.json

# Android
*.apk
*.aab
*.keystore

# Local NuGet override (see NuGet.config) -- a stopgap Meowshell package built
# from a commit ahead of what its own CI has published, never a real release
nupkg/*.nupkg
nupkg/*.snupkg
